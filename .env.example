# PocketBase URL
# Local development
POCKETBASE_URL=http://127.0.0.1:8090

# Production - PocketHost (free tier)
# NEXT_PUBLIC_POCKETBASE_URL=https://your-instance.pockethost.io

# Production - Self-hosted (optional)
# NEXT_PUBLIC_POCKETBASE_URL=https://yourdomain.com

# PocketBase Admin (for local db:reset script)
PB_ADMIN_EMAIL=your-email@example.com
PB_ADMIN_PASSWORD=your-password-here

# Firebase Phone Auth (for SMS OTP verification)
# Get these values from Firebase Console > Project Settings > General > Your apps > Web app
# 1. Go to https://console.firebase.google.com
# 2. Create new project (or use existing)
# 3. Enable Authentication > Sign-in method > Phone
# 4. Add your domains to authorized domains (localhost for dev, your-domain.com for prod)
# 5. Go to Project Settings > General > Your apps > Add web app
# 6. Copy the config values below
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id

# AI Product Creation (optional - enables "Snap to Add" feature)
# OpenAI API Key for product identification using GPT-4o Mini Vision
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# Replicate API Token for emoji icon generation using sdxl-emoji
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your-replicate-api-token
